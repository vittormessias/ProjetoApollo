<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projetoapollo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <!-- iniciando com o splash -->
        <activity
            android:name=".Splash_Activity"
            android:theme="@style/temaSemBarra">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Segunda janela -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/temaSemBarra" />
        <!-- janela de login -->
        <activity
            android:name=".LoginPublico_Activity"
            android:theme="@style/temaSemBarra" />
        <activity
            android:name=".LoginArtista_Activity"
            android:theme="@style/temaSemBarra" />
        <!-- janela inscrição -->
        <activity
            android:name=".TelaInscricao_Activity"
            android:theme="@style/temaSemBarra" />
        <activity
            android:name=".CadPerfilArtista_Activity"
            android:theme="@style/temaSemBarra" />
        <activity
            android:name=".CadPerfilPublico_Activity"
            android:theme="@style/temaSemBarra" />
        <!-- janela splash artista e publico -->
        <activity
            android:name=".SplashArtista_Activity"
            android:theme="@style/temaSemBarra" />
        <activity
            android:name=".SplashPublico_Activity"
            android:theme="@style/temaSemBarra" />
        <!-- janela Home -->
        <activity android:name=".HomePublico_Activity" />
        <activity android:name=".HomeArtista_Activity" />
        <!-- janela de Decisão -->
        <activity
            android:name=".TelaArtistaPublico_Activity"
            android:theme="@style/temaSemBarra" />
        <!-- janela de estilos -->
        <activity
            android:name=".EstiloMusicalArtista_Activity"
            android:theme="@style/temaSemBarra" />
        <activity
            android:name=".EstiloInstrumento_Activity"
            android:theme="@style/temaSemBarra" />
        <activity
            android:name=".EstiloMusicalPublico_Activity"
            android:theme="@style/temaSemBarra" />
    </application>

</manifest>